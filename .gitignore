# Dependencies
node_modules/

# Build output
.next/
dist/
*.tsbuildinfo

# Secrets — never commit
.env
.env.local
.env.*.local

# Local SQLite scratch DBs (we use Postgres)
prisma/*.db
prisma/*.db-journal

# Editor / OS noise
.DS_Store
.vscode/
.idea/
*.swp

# Misc
*.log
.turbo/
coverage/


# Local DB backups (contain personal data)
backups/

# Personal application data (seed spreadsheets) — never publish
*.csv
*.xlsx

# Claude Code local config
.claude/

# Local seed config (maps your CSV's column names — may contain real names)
seed.config.json
